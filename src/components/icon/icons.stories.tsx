import React from 'react';
import { storiesOf } from '@storybook/react';

import { IconsTable } from '../../../.storybook/storybook-components';

storiesOf('Icons', module).add('Icons', () => (
  <IconsTable />
));
